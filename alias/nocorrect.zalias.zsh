# Alice's over-engineered z-shell configuration, released in the public domain.
# Aliases for commands that should not have spellchecking applied to them.
# Often this is because they target files which they intend to create.

# XXX: Proper completions files should cover this?
alias cp='nocorrect cp'
alias mv='nocorrect mv'
alias touch='nocorrect touch'
