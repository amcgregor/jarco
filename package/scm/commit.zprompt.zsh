function _jarco_prompt__scm_ref {
	# Render the SCM's "current commit reference", whatever that may be.
	# Some systems have the concept of a detached state; status color choice determined by this.
}
